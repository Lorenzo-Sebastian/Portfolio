function createPlayer(hp, name, move){
    var hp = hp;
    var name = name;
    var move = move;
    return function () {
        return hp;
        return (name);
       
    }
}
lonzo = createPlayer(150, "richtofen")

console.log(lonzo.function());