function createPlayer(hp, name, move){
    var hp = hp;
    var name = name;
    var move = move;
    return function () {
        return hp;
        return (name);
        for (var i = 1; i <= 5; i ++){

        }
    }
}

var results = createPlayer(150,"Richtofen", 300 )
console.log(results());